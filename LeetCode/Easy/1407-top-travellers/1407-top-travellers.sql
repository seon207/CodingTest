select name, ifnull(b.distance,0) as travelled_distance from users a left join (select user_id, sum(distance) distance from rides group by user_id) b on a.id = b.user_id order by distance desc, name asc;