select a.product_id, a.product_name, b.amount * a.price as total_sales from food_product a join (select product_id, sum(amount) amount from food_order where produce_date like '2022-05%' group by product_id) b on a.product_id = b.product_id order by total_sales desc, product_id;